<!--
 @title Roller
 @brief Infinite scrolling, a visible row count, and subject binding.

 `options` takes the list plus a mode parameter (`options-mode="infinite"`)
 so it loops endlessly. `visible_row_count` sets how many rows are shown,
 and `bind_value="subject_index"` keeps the selection in a subject that the
 label below mirrors.
-->
<screen>
	<styles>
		<style name="style_roller" bg_color="0x0f172a" bg_opa="100%" text_color="0x94a3b8" radius="8" pad_all="6" />
		<!-- The centered (selected) row -->
		<style name="style_roller_sel" bg_color="0x3b82f6" text_color="0xffffff" />
	</styles>

	<view>
		<lv_roller
			align="center"
			options="January&#10;February&#10;March&#10;April&#10;May&#10;June&#10;July&#10;August&#10;September&#10;October&#10;November"
			options-mode="infinite"
			visible_row_count="3"
			bind_value="subject_index"
		>
			<style name="style_roller" />
			<style name="style_roller_sel" selector="selected" />
		</lv_roller>

		<lv_label align="center" y="-70" bind_text="subject_index" bind_text-fmt="Month index: %d" />
	</view>
</screen>
