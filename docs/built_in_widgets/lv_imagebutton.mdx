---
title: "Image Button (lv_imagebutton)"
description: "A button that draws per-state images instead of a styled rectangle."
---

## Overview

The Image Button behaves like a [Button](./lv_button) but draws user-supplied images per state instead of a styled rectangle. Separate left, middle and right images can be set; the middle image is tiled to fill the width.

Learn more in the [LVGL Open documentation](https://lvgl.io/docs/open/widgets/imagebutton).

<LvglExample name="lv_example_imagebutton" path="screens/lv_example_imagebutton" />

{/* @generated from lv_xml/xmls/widgets/lv_imagebutton.xml — do not edit below this line; re-run docs/scripts/gen_widget_api.ts instead. */}

## Enumerations

**`lv_imagebutton_state`** — Possible states of an image button

| Value | Description |
| --- | --- |
| `released` | Released |
| `pressed` | Pressed |
| `disabled` | Disabled |
| `checked_released` | Checked and released |
| `checked_pressed` | Checked and pressed |
| `checked_disabled` | Checked and disabled |

## Properties

Properties below are the widget's XML [`<api>`](../syntax/api); see [API](../syntax/api) for how properties, parameters and elements work.

| Property | Type | Description |
| --- | --- | --- |
| `state` | `enum:lv_imagebutton_state` | The current state of the imagebutton |

### `<lv_imagebutton-src_left>`

Set the left image source in a given state

_access `set`_

| Name | Kind | Type | Description |
| --- | --- | --- | --- |
| `state` | arg | `enum:lv_imagebutton_state` | Set the image in this state |
| `src` | prop | `image` | The image in the given state |

### `<lv_imagebutton-src_right>`

Set the right image source in a given state

_access `set`_

| Name | Kind | Type | Description |
| --- | --- | --- | --- |
| `state` | arg | `enum:lv_imagebutton_state` | Set the image in this state |
| `src` | prop | `image` | The image in the given state |

### `<lv_imagebutton-src_mid>`

Set the middle image source in a given state

_access `set`_

| Name | Kind | Type | Description |
| --- | --- | --- | --- |
| `state` | arg | `enum:lv_imagebutton_state` | Set the image in this state |
| `src` | prop | `image` | The image in the given state |
